# 本项目用 npm 而非 pnpm 安装依赖。
#
# 背景：pnpm 默认拦截第三方包的构建脚本，本项目依赖链中的 esbuild 被拦截后，
# 依赖树处于半安装状态（包被放进 .ignored_* 目录、传递依赖缺失），Vite 无法运行。
# npm 采用扁平目录且默认执行构建脚本，在该环境下可正常完成安装。
#
# 若后续仍想使用 pnpm，需先执行 `pnpm approve-builds` 放行 esbuild。
registry=https://registry.npmmirror.com
